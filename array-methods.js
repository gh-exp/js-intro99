const arr = ['Mouse', 'Laptop'];

// push() method is used to add an element to the tail-end of the array

arr.push('Phone');

console.log(arr);

// pop() method to remove the last elements

arr.pop();

console.log(arr);
